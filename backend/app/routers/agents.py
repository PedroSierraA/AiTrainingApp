from fastapi import APIRouter

router = APIRouter(prefix="/agents", tags=["agents"])

# Endpoints pendientes:
# POST /weekly/start    (Paso 10)
# POST /weekly/confirm  (Paso 10)
# POST /analyzer/run    (Paso 11)
